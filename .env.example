# Chave da Anthropic — geração das respostas e LLM-as-judge
ANTHROPIC_API_KEY=

# Vector store (se optar por Postgres + pgvector em vez de ChromaDB local)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/copom_rag

# Configuração do serviço
# EVAL_THRESHOLD=0.70
# GENERATION_MODEL=claude-haiku-4-5
# JUDGE_MODEL=claude-sonnet-4-5
